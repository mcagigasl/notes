[[Pandas DataFrame|Dataframes]] can be queried with [[SQL]] statements using the `.query method`

```
dataFrame.query('where clause')
```

The SQL clause doesn't contain neither the Select or the From statements.